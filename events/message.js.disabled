
const meta = {
    name: "Message",
    description: `Parses and processes every message received by the bot`
}

function run(client, args)
{
    
}

module.exports.meta = Object.freeze(meta);
module.exports.run = run;
